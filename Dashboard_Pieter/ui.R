# Step 1: Customers --> Purchase history --> Loyalty

# Libraries
library(shiny)
library(shinydashboard)

# Shiny UI
shinyUI(
  dashboardPage(
    dashboardHeader(title = "Menu"),
    dashboardSidebar(),
    dashboardBody()
  )
)